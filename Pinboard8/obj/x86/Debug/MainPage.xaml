<Page
    x:Class="Pinboard8.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pinboard8"
    xmlns:converters="using:Pinboard8.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:DateElapsedTimeConverter x:Key="dataConverter"></converters:DateElapsedTimeConverter>
    </Page.Resources>
    
    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}" >

        <ScrollViewer HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Auto">

            <StackPanel Orientation="Horizontal" Margin="20, 50, 20, 50">

                <StackPanel MaxWidth="450" Margin="20,10,10,10" Width="466" Height="884">
                    <TextBlock FontSize="42" Text="Recent Bookmarks" Margin="0,0,0,20"></TextBlock>
                    <ListBox x:Name="RecentBookmarks" ItemsSource="{Binding RecentBookmarks}" Width="413">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid  Margin="0,0,0,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="40" />
                                </Grid.RowDefinitions>
                                    <HyperlinkButton  Grid.Row="0" Content="{Binding Title}" NavigateUri="{Binding Url}" />
                                    <Grid Grid.Row="1" >
                                        <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                                        <ColumnDefinition Width="150"></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                        <ListBox Grid.Column="0" ItemsSource="{Binding Tags}" BorderThickness="1" BorderBrush="Yellow">
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal"></StackPanel>
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" FontStyle="Italic"></TextBlock>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                        <Border Grid.Column="1" BorderThickness="1" BorderBrush="Yellow"  HorizontalAlignment="Right" >
                                            <TextBlock Text="{Binding Time, Converter={StaticResource dataConverter}}" TextAlignment="Right"></TextBlock>
                                        </Border>
                                    </Grid>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                </StackPanel>


                <StackPanel Margin="20,10,10,10" MinWidth="884">
                    <TextBlock FontSize="42" Text="My Tags" Margin="0,0,0,20"></TextBlock>
                    <ListView ItemsSource="{Binding Tags}" ScrollViewer.VerticalScrollBarVisibility="Disabled" 
                        ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollMode="Disabled"
                        ScrollViewer.HorizontalScrollMode="Auto" ScrollViewer.ZoomMode="Disabled" Height="814">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapGrid Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <HyperlinkButton x:ConnectionId='1' Width="150" Height="75"  Background="Blue" Foreground="White"                     
                               Content="{Binding Name}">
                                </HyperlinkButton>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

    </Grid>
</Page>

